# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: 'NEURA Dashboards'
    orgId: 1
    folder: 'NEURA'
    folderUid: 'neura'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

