# Avatar Service Deployment (GPU)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neura-avatar-service
  namespace: neura
  labels:
    app: neura-avatar-service
    tier: gpu-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: neura-avatar-service
  template:
    metadata:
      labels:
        app: neura-avatar-service
        tier: gpu-service
    spec:
      containers:
        - name: avatar-service
          image: avatar-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8002
              name: http
          envFrom:
            - configMapRef:
                name: neura-config
            - secretRef:
                name: neura-secrets
          env:
            - name: AVATAR_MODEL_PATH
              value: "/app/models/avatar"
          resources:
            requests:
              cpu: "1000m"
              memory: "4Gi"
              nvidia.com/gpu: "1"
            limits:
              cpu: "4000m"
              memory: "16Gi"
              nvidia.com/gpu: "1"
          volumeMounts:
            - name: avatar-models
              mountPath: /app/models/avatar
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 120
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: avatar-models
          persistentVolumeClaim:
            claimName: avatar-models-pvc
      tolerations:
        - key: "nvidia.com/gpu"
          operator: "Exists"
          effect: "NoSchedule"
      nodeSelector:
        accelerator: nvidia-gpu
---
# Avatar Models PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: avatar-models-pvc
  namespace: neura
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: fast-ssd
  resources:
    requests:
      storage: 10Gi

